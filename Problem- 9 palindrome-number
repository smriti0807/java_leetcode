class Solution {
    public boolean isPalindrome(int x) {
        int c,r,s=0;
        c=x;
        while(x>0)
        {
            r=x%10;
            s=r+(s*10);
            x=x/10;
        }
        return (c==s) ;
           
    }
}
