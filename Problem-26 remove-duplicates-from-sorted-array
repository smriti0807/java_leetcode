class Solution {
    public int removeDuplicates(int[] nums) {
        int i=0 , k , j;
        for(j=1;j<nums.length;j++)
        if(nums[i]!=nums[j])
        {
           i++;
         nums[i]=nums[j];
        }
        k = i+1;
        return k;
    }
}
